import React from "react";
import { Text, SafeAreaView } from "react-native";

const Settings = () => {
  return (
    <SafeAreaView>
      <Text>Settings</Text>
    </SafeAreaView>
  );
};

export default Settings;
